import React from 'react';
import { favorites } from '../styles/jobList.scss';

const Favorites = () => {
    return (
        <div className={favorites}>
            <h1>Favorites</h1>
            This is empty right now
        </div>
    );
};


export default Favorites;
